name: Generate Pac-Man contribution graph (disabled)

on:
  workflow_dispatch:  # apenas manual; sem schedule, sem push

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pac-Man fixado como arquivo estático. Nada a gerar."
